{
  "name": "rvfs-server-node",
  "version": "0.1.0",
  "description": "RVFS reference server implementation for Node.js (Fastify)",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "fastify": "^4.0.0",
    "zod": "^3.0.0",
    "rvfs-types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
