{
  "name": "rvfs-types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types for the RVFS packages — no runtime code",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.7.0"
  }
}

